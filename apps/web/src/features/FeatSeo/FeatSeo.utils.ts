import { type MetaAudio, type MetaImage, type MetaVideo } from './FeatSeo.types';

/**
 * Metadata Utils
 */

export const mapImageToMetadata = ({ url, alt }) => {};
export const mapVideoMetadata = ({ url }) => {};
export const mapAudioToMetadata = ({ url }) => {};
export const mapAuthorToMetadata = ({ name, url }) => {};

/**
 * JSON-LD Utils
 */
export const getPublisherJsonLd = () => {};
export const mapImageToJsonLd = ({ url, alt }) => {};
export const mapVideoToJsonLd = ({ url }) => {};
export const mapAudioToJsonLd = ({ url }) => {};
export const mapAuthorToJsonLd = ({ name, url }) => {};
