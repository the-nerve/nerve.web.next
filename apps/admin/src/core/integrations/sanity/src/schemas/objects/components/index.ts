import { hero, heroWithImage, simpleHero, simpleHeroWithImage } from './hero';
import { footerNav, headerNav, navItem } from './navigation';
import { subscribe } from './subscribe';

export const components = [
  navItem,
  headerNav,
  footerNav,
  hero,
  heroWithImage,
  simpleHero,
  simpleHeroWithImage,
  subscribe,
];
